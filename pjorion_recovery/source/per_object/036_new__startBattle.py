# recovered via pycdc

old__startBattle(self)
if mody.GLOBAL_ENABLE:
    pass
mody.modEnable = getResolut()
if mody.GLOBAL_ENABLE and getResolut():
    BigWorld.player().arena.onVehicleKilled += onVehicleKilled
    mody.loadConf()
    if mody.interactivSettings:
        LoadConfLoop()
    mody.batleStart = True
    DrawControl()
