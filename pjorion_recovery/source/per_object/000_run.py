# recovered via pycdc

if stat == 'serv':
    thread = Thread(target = self.query, name = 'ServerCheck')
    thread.start()
    return None
if stat.lower() == 'full':
    if stat.lower() == 'test':
        mody.SERVER_RES = (None, True, date, PACK_VER, '3')
        continue
    if stat.lower() == 'trial':
        continue
    if stat.lower() == 'full':
        continue
    continue
continue
