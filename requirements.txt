# Все зависимости опциональны. Программа работает на чистой stdlib.
# requests подхватывается автоматически, если установлен — даёт чуть
# более вежливые HTTPS-ошибки. Без него используется urllib.
requests>=2.28; python_version >= "3.7"
